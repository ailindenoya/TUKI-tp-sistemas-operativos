#include "../../kernel/include/pcb.h"
#include "../include/cpu_config.h"
//t_pcb* contextoDeEjecucion;

void set(registro, valor){
    sleep(cpu_obt);
    //Ver tamanio del registropara determinar iteraciones para llenar registro.
    switch(sizeof(valor)){
        case 4:
        break;
        case 8:
        break;
        case 16:
        break;
        default:
        //Mensaje de tamaño incorrecto o incopatibilidad con el registro.
    }
}

t_pcb* yield(){

}

void exit()